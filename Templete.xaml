<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <TextBlock x:Name="Title" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,12" HorizontalAlignment="Center"/>
    <StackPanel  Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,12" HorizontalAlignment="Center" Spacing="12">
        <Border x:Name="Cover" Width="150" Height="150" Margin="0,0,12,0" VerticalAlignment="Top" HorizontalAlignment="Right"/>
        <StackPanel Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Left" Spacing="8">
            <TextBlock x:Name="Artist" FontSize="18"/>
            <TextBlock x:Name="Album" FontSize="18"/>
            <TextBlock x:Name="Format" FontSize="18"/>
            <TextBlock x:Name="Level" FontSize="18"/>
        </StackPanel>
    </StackPanel>
    <Border x:Name="AudioPlayer" Grid.Row="2" Margin="0,12,0,0" HorizontalAlignment="Center"/>
    <Button x:Name="DownloadButton" Content="обть" Grid.Row="3" Width="200" HorizontalAlignment="Center" Click="OnDownloadClick" Margin="0,12,0,0"/>
    <Border x:Name="StatusArea" Grid.Row="4" Margin="0,12,0,0" HorizontalAlignment="Center"/>
</Grid>